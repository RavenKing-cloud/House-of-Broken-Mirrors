/// @description Insert description here
// You can write your code in this editor
if global.progress >= req && alarm[0] = -1 alarm[0] = 60;
if decay{
	image_alpha -= 0.05;
	if image_alpha < 0.1 instance_destroy();
}