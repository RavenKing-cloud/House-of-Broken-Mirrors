// Change this to the room you want to go to
room_goto(Area1); // Or use a specific room like: room_goto(rm_Level2);

// Optional: move player to a specific position in the next room
// Use a global variable to store where the player should appear
//global.spawn_x = 100;
//global.spawn_y = 150;
